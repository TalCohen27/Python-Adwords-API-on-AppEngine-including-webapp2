import webtest

import hello_world


def test_get():

    app = webtest.TestApp(hello_world.hello_world_app)

    response = app.get('/')

    assert response.status_int == 200
    assert response.body == 'Hello, World!'
